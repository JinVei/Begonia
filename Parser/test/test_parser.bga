

var kelo = 1;
var meiko str;

func name (a int) void;

func name (a int) void {
    kelo = (1+2-3*90/10&da)^90|oi&10;
}
name3();
name(2);

if no {
    print("hello!");
    return r1;
}
k+1; o|1;
;
func testWhile(uk="q") int
{
    his_6name1 = ho;
    exit = false;
    while is == true || no && run {
        doSomething(args,shoot,handler);
        if exit {
            exit(0);
        }
    }
    if tryCondition() {
        print("hi");
    }
    if !my || (you + he) && his_son {
        kalo = say(moon) + damn * 2 +1;
    } 
    return v1,r2, k3;
}
